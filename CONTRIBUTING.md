# 贡献指南

## 欢迎贡献

我们欢迎任何形式的贡献：提 Issue、写 PR、补充 reference、改进样例、修正笔误、分享使用案例。

## 贡献前的必读

**1. 本仓库学的是「方法论」不是「行业知识」**

每份 reference 文件的抽象层（`## 怎么分析` / `## 怎么写` / `## 及格线`）必须是**跨领域通用**的。

- ✅ 通用表述：「核心用户角色」「购买单位画像」「该领域的滩头区域」「角色-资源-动作矩阵」
- ❌ 行业表述：「化学教师」「湖南省高中」「mhcmen 渲染」「化学知识图谱节点」

> 详见 [docs/02-anti-plagiarism.md](docs/02-anti-plagiarism.md)

**2. 「方法 + 案例合一」是硬规范**

每份 reference 必须既包含方法论骨架（怎么分析、怎么写），又包含至少一个抽象化的案例（不能只给方法不给例子）。

**3. 五段式结构是 reference 的强制模板**

每份 reference 必须有这五段（顺序固定）：

```
## 回答什么问题
## 怎么分析 / 怎么设计
## 怎么写（文档结构） / 文档结构
## 及格线        （☑ 清单 4–6 条）
## 最易挂的坑    （2–4 条）
```

开头必须有一行 `> 源文档：XX-XXX.md` 标注（如适用）。

**4. 样例必须显式标注「本文件是示范参考，不是模板」**

任何 `_example-*.md` 文件，开头必须声明这一点，并说明样例来源（如「沉淀自作者 ChemAI 真实项目，已去行业化处理」）。

## 提 PR 的流程

1. **Fork** 本仓库
2. **新建分支**：`git checkout -b feat/your-skill-name`
3. **编辑 / 新增文件**：遵守上述 4 条硬规范
4. **本地跑生成器**（如改动了能力地图数据）：
   ```bash
   python3 tools/capability-map-gen.py
   # 检查 docs/images/capability-map.svg 无溢出
   ```
5. **Commit**：`git commit -m "feat: 新增 XXX 段位的 XXX reference"`
6. **Push**：`git push origin feat/your-skill-name`
7. **提 PR**：描述变更动机、影响的段位、是否需要更新能力地图

## 重大变更（新增段位 / 新增子技能）

**不要直接提 PR**，先开 Issue 讨论：
- 为什么需要这个段位？
- 它是独立可复用的方法论吗？（有自己的框架 + 触发词 + 产出物）
- 它能被拆成独立的子 skill 吗？
- 它与现有 8 段位如何衔接？

经 Review 同意后，再按上面 4 条硬规范提交。

## 提 Issue 的场景

- 报告 Bug（如生成器出错、reference 描述有误）
- 提议新段位 / 新子技能
- 提议方法论改进
- 报告抄袭 / 侵权嫌疑
- 提问、讨论、不确定是否要提 PR

## 反抄袭与法律

如果你发现本仓库内容涉嫌抄袭或侵权：
- **不要直接公开评论**，先发 Issue 或发邮件给作者
- 作者承诺 24 小时内响应
- 涉及侵权的文件会被立即删除或修改

详见 [NOTICE](NOTICE) 与 [docs/02-anti-plagiarism.md](docs/02-anti-plagiarism.md)。

## 行为准则

请阅读并遵守 [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)。所有贡献者、Reviewer、维护者都应保持友善、专业的沟通环境。
